<%= for post <- curated_posts(assigns) do %>
  <section>
    <a href="<%= post_url(post) %>">
      <h2><%= title(@editorial) %> @<%= post.author.username %></h2>
      <img src="<%= post_image_url(post) %>" alt="<%= title(@editorial) %>" />
    </a>
  </section>
<% end %>
