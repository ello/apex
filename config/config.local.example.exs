use Mix.Config

# Local overrides for config settings in dev/test.
# To use simply copy and customize:
#     cp config/config.local{.example,}.exs

# Example: Override local database credentials
# config :ello_core, Ello.Core.Repo,
#   username: "cat",
#   password: "yolnt"

# Example: Override local dev port
# if Mix.env == :dev do
#   config :ello_dispatch, Ello.Dispatch.Endpoint,
#     http: [port: 6670]
# end
